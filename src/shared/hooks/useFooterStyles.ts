const useFooterStyles = () => {
    const styles = {
        footer: {
            "& >.MuiGrid-container": {
                height: 30,
            },
        },
    };

    return { styles };
};

export default useFooterStyles;
